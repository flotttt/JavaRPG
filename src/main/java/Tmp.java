import java.util.Comparator;

public class Tmp {
/*        this.items.sort(
                Comparator.comparingInt(MenuItem::getChoiceValue)
        );

        this.items.removeIf(o -> !o.isEnabled());
        this.numberOfChoices = this.items.size();
        */
}
